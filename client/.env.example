# Frontend environment — copy to .env.local for local dev
# In Docker, the nginx proxy routes /api → server container automatically

# API base URL (used by Vite dev server proxy; in Docker nginx handles this)
VITE_API_URL=/api/v1
